<Window x:Class="LayerComparer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LayerComparer"
        xmlns:dd="urn:gong-wpf-dragdrop"
        mc:Ignorable="d"
        Title="MainWindow" Height="560" Width="1200"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        >
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Margin="5,10" Text="Select source:" />
            <TextBox TextWrapping="NoWrap" Margin="5,10" Text="{Binding DatasourceOne, Mode=TwoWay}" Width="300" Drop="TextBoxLeft_Drop" PreviewDragOver="TextBox_PreviewDragOver" />
            <TextBlock Margin="5,10" Text="Select compare:" />
            <TextBox TextWrapping="NoWrap" Margin="5,10" Text="{Binding DatasourceTwo, Mode=TwoWay}" Width="300" Drop="TextBoxRight_Drop" PreviewDragOver="TextBox_PreviewDragOver"/>
        </StackPanel>
        <Button Content="Load Sources" Margin="5,10" HorizontalAlignment="Right" Click="LoadSources_Click"/>
        <TabControl Name="TabControl" Grid.Row="1">
            <TabItem Header="Overview">
                <Grid Background="#FFE5E5E5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <local:LayerTable x:Name="dgLayersLeft" DataContext="{Binding LayersLeft}" LinkedWith="{Binding ElementName=dgLayersRight, Path=DataGridSource}" />
                    <local:LayerTable x:Name="dgLayersRight" DataContext="{Binding LayersRight}" Grid.Column="1" />

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
                        <Button Margin="5" HorizontalAlignment="Right" Content="Compare selected layers" Click="Compare_Click" />
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Name="OutputTab" Header="Output" IsEnabled="False">
                <ScrollViewer Name="OutputScrollViewer">
                    <RichTextBox Block.LineHeight="1" IsReadOnly="True" Name="OutputBox" />
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
